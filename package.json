{
  "name": "elmDemo",
  "version": "0.0.1",
  "description": "Full-stack Elm application based on Board ",
  "main": "main.js",
  "scripts": {
    "start": "nodemon --exec \"elm make src/main.elm --output dist/main.js && node local.js\"",
    "front": "elm make src/app.elm --output public/app.js",
    "reactor": "elm reactor",
    "install": "elm-install install",
    "back": "elm make src/main.elm --output=dist/main.js",
    "elm": "elm"
  },
  "author": "airtucha",
  "license": "ISC",
  "dependencies": {
    "elm": "^0.18.0",
    "object-hash": "^1.3.1"
  },
  "devDependencies": {
    "nodemon": "^1.12.1",
    "elm-github-install": "^1.6.1"
  }
}
